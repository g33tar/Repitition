var pound = ''; 						// called a variable of nothing
  while(pound.length< 8){		//while the variable length is less than 8, the loop will continue
    pound = pound + '#';		//this is how each round of the loop will increase by one in length
  console.log(pound);				//directions to be read for variable
}
